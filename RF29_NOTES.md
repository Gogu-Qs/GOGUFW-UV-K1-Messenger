# RF29

Base: RF28.

Changes:
- Removed RX-side controlled re-prime; TX-after re-prime remains.
- ACK text remains hidden from Inbox.
- ACK success schedules a single short beep after the `+` status update.
- Compose / Reply / Draft send / Sent resend now returns to Sent list.
- Sent read screen shows the same status marker (`?`, `+`, `x`) beside the message ID.
- Inbox/Sent read counter and numeric T9 from previous builds are preserved.
