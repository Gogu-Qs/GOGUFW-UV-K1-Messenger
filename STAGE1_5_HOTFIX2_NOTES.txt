GGFW Clean 5.5.0 Stage 1-5 Hotfix2

Scope: UI alignment only. RF/FSK, normal radio RX/TX, T9 logic and storage logic are unchanged from Hotfix1.

Changes:
- Messenger HOME title remains centered.
- Messenger HOME menu rows (INBOX / COMPOSE / SENT / DRAFTS / SETTINGS) restored to left alignment.
- Sub-screen item counters such as 1/2, 0/0 are now right-aligned on the header/status line.
- Settings cursor/scroll behavior from Hotfix1 retained.
- VS Code Docker build tasks retained.

Build:
- VS Code: Cmd+Shift+B -> GGFW: Build Fusion (Docker)
- Terminal: bash compile-with-docker.sh Fusion
